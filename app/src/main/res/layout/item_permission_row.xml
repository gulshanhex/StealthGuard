<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_permission_row.xml -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="1dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <!-- Status icon (✅ / ❌ / ⚠️) -->
        <TextView
            android:id="@+id/tvPermIcon"
            android:layout_width="32dp"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:gravity="center"
            android:text="✅" />

        <!-- Label + description column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="8dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvPermLabel"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#212121"
                    android:text="Permission Name" />

                <TextView
                    android:id="@+id/tvPermStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:text="Granted"
                    android:layout_marginStart="8dp" />

            </LinearLayout>

            <TextView
                android:id="@+id/tvPermDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="#757575"
                android:layout_marginTop="3dp"
                android:text="Description of why this permission is needed." />

        </LinearLayout>

        <!-- Fix button — hidden when granted -->
        <Button
            android:id="@+id/btnFixPermission"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Grant"
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="@color/colorPrimary"
            android:paddingHorizontal="12dp"
            android:visibility="gone" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
